import { twMerge } from "tailwind-merge";

export default function GradientButton({ className }: { className?: string }) {
  return (
    <div className="w-[200px] h-10 bg-red-400 rounded-lg">
        A
    </div>
  );
}
